# Sentinel
An attempt to document workflow and tests needed...
## Setup
- useful commands
  - Open up browser to record a test
    ```
    npx playwright codegen --output=payment.spec.ts http://localhost:5173 --headed
    ```
  - Run the tests locally
    ```
    npx playwright test
    ```
